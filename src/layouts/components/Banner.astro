---
import { markdownify } from "@/lib/utils/textConverter";
import { Image } from "astro:assets";
import Stars from "./Stars.astro";
const { banner } = Astro.props;
const { title, button, image, content } = banner;
---

<section
  class="section banner relative min-h-[656px] flex flex-col justify-start items-center pt-24"
  style="background: #000;"
>
  {/** Estrellas aleatorias */}
  <Stars />
  <div class="banner-fade-bottom"></div>
  <div class="container relative z-10">
    <div class="w-full flex flex-col items-center text-center">
      <h1
        class="banner-title"
        style="color:#c1d2e694; font-size:3rem;"
        set:html={markdownify(title)}
      />
      <p
        class="mt-6 banner-title-content"
        style="color:#c1d2e694; font-size: 1.2rem; font-weight: 200;"
        set:html={markdownify(content)}
      />
      {
        button.enable && (
          <a class="btn btn-white mt-8" href={button.link}>
            {button.label}
          </a>
        )
      }
    </div>

    <!-- <div class="row items-center">
      <div class="lg:col-6">
        <h1
          class="banner-title"
          style="color:#c1d2e694"
          set:html={markdownify(title)}
        />

        <p
          class="mt-6 banner-title-content"
          style="color:#c1d2e694"
          set:html={markdownify(content)}
        />

        {
          button.enable && (
            <a class="btn btn-white mt-8" href={button.link}>
              {button.label}
            </a>
          )
        }
      </div>
      <div class="lg:col-6">
        <Image
          class="w-full object-contain"
          alt="banner"
          src={image}
          width={603}
          height={396}
        />
      </div>
    </div> -->
  </div>

  <!-- <Image
    class="banner-shape absolute -top-28 right-0 -z-[1] w-full max-w-[30%]"
    alt="banner"
    src="/images/banner-shape.png"
    width={496}
    height={466}
  /> -->
</section>
